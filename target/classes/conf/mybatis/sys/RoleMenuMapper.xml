<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyt.xjj.anes.dao.sys.RoleMenuMapper">

	<sql id="selectColumns">
		ID,
		ROLE_ID as roleId,
		MENU_ID as menuId
	</sql>
	
	<!-- 插入列名 -->
	<sql id="insertColumns">
		<trim suffix="" suffixOverrides=",">
			<if test="id != null">ID,</if>
			<if test="roleId != null">ROLE_ID,</if>
			<if test="menuId != null">MENU_ID</if>
		</trim>
	</sql>
	<!-- 插入列值 -->
	<sql id="insertValues">
		<trim suffix="" suffixOverrides=",">
			<if test="id != null">#{id},</if>
			<if test="roleId != null">#{roleId},</if>
			<if test="menuId != null">#{menuId}</if>
		</trim>
	</sql>

	<insert id="insert" keyProperty="id" parameterType="roleMenu">
		INSERT INTO T_SYS_ROLE_MENU(
		<include refid="insertColumns" />
		)
		VALUES(
		<include refid="insertValues" />
		)
	</insert>

	<insert id="batchInsert" keyProperty="id" parameterType="list">
		INSERT INTO T_SYS_ROLE_MENU(
		ID, ROLE_ID,MENU_ID
		)
		VALUES
		<foreach collection="list" item="item" index="index"
			separator=",">
			(#{item.id},#{item.roleId},#{item.menuId})
		</foreach>
	</insert>

	<delete id="deleteByRoleId" parameterType="string">
		DELETE FROM T_SYS_ROLE_MENU	WHERE ROLE_ID = #{id}
	</delete>

	<select id="select" parameterType="string" resultType="roleMenu">
		SELECT
		<include refid="selectColumns" />
		FROM T_SYS_ROLE_MENU WHERE ID=#{id}
	</select>

	<select id="selectByRoleId" parameterType="string" resultType="string">
		SELECT A.MENU_ID FROM T_SYS_ROLE_MENU A
		JOIN T_SYS_ROLE B on(A.ROLE_ID=B.ID)
		JOIN T_SYS_MENU C on(C.ID = A.MENU_ID)
 		WHERE A.ROLE_ID=#{roleId} AND C.STATUS='ON'
	</select>


</mapper>